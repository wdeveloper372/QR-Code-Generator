# QR-Code-Generator

A Python utility program that generates custom QR codes with support for embedded center logos.

## Features
* Instantly generates customized QR codes from input text or links.
* Advanced functionality allowing users to embed a custom image or logo directly into the center of the QR code.
* Dependency management included via `requirements.txt` for easy setup.

## Tech Stack
* Python
